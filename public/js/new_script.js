// Fonction qui calcule la somme de deux nombres
function sum(a, b) {
	// Retourne la somme de a et b
	return a + b;
}

// Exporte la fonction sum pour qu'elle soit accessible dans d'autres fichiers
module.exports = sum;
